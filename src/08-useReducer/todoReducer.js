
export const todoReducer = (initialState = [], action) => {

    switch (action.key) {
        case 'ABC':

            return initialState;

        default:
            return initialState;
    }
};